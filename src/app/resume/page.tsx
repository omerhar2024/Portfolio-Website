'use client'

import { ResumeAccordion } from '@/components'
import styles from './page.module.css'

export default function ResumePage() {
  return (
    <main className={styles.main}>
      <ResumeAccordion />
    </main>
  )
} 